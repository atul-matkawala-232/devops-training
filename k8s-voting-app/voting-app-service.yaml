apiVersion: v1
kind: Service
metadata:
  name: voting-service
  namespace: vote
  labels:
    app: voting-app
spec:
  type: NodePort
  selector:
    app: voting-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30004